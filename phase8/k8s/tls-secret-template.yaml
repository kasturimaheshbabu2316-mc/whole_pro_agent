apiVersion: v1
kind: Secret
metadata:
  name: tls-secret
  namespace: whole-agent
type: kubernetes.io/tls
data:
  tls.crt: <base64-encoded-cert>
  tls.key: <base64-encoded-key>

# Replace the placeholders with your TLS certificate and key.
