filebeat.inputs:
  - type: container
    paths:
      - /var/log/containers/*.log
    processors:
      - add_kubernetes_metadata:
          host: ${NODE_NAME}
          matchers:
            - type: container
              hints.enabled: true

output.logstash:
  hosts: ["logstash:5044"]
