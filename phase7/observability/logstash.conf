input {
  beats {
    port => 5044
  }
}

filter {
  json {
    source => "message"
    target => "json"
    add_tag => ["json_parsed"]
    remove_field => ["message"]
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "whole-agent-logs-%{+YYYY.MM.dd}"
  }
}
